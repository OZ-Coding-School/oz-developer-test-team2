# 개발자 성격 테스트 API 명세서

**Base URL:** `http://localhost:3000`

---

## 1. 전체 질문 목록 조회

### `GET /api/questions`

모든 테스트 질문을 반환합니다.

**Response**

- Status: `200 OK`

```json
[
  {
    "id": 1,
    "text": "새로운 프로젝트를 시작할 때 가장 먼저 하는 일은?",
    "options": [
      {
        "text": "사용자가 볼 화면부터 디자인한다",
        "type": "frontend"
      },
      {
        "text": "데이터베이스 구조를 먼저 설계한다",
        "type": "backend"
      }
    ]
  }
]
```

---

## 2. 특정 질문 조회

### `GET /api/questions/:id`

ID에 해당하는 질문을 반환합니다.

**Path Parameters**

| 파라미터 | 타입 | 설명 |
|---------|------|------|
| `id` | number | 질문 ID (1~10) |

**Response**

- Status: `200 OK`

```json
{
  "id": 1,
  "text": "새로운 프로젝트를 시작할 때 가장 먼저 하는 일은?",
  "options": [
    {
      "text": "사용자가 볼 화면부터 디자인한다",
      "type": "frontend"
    },
    {
      "text": "데이터베이스 구조를 먼저 설계한다",
      "type": "backend"
    }
  ]
}
```

**Error Response**

- Status: `404 Not Found`

```json
{
  "error": "해당 질문을 찾을 수 없습니다."
}
```

---

## 3. 특정 개발자 타입 결과 조회

### `GET /api/results/:type`

개발자 타입에 해당하는 결과를 반환합니다.

**Path Parameters**

| 파라미터 | 타입 | 설명 |
|---------|------|------|
| `type` | string | 개발자 타입 |

**유효한 타입 목록**

| 타입 | 설명 |
|------|------|
| `frontend` | 프론트엔드 벌 |
| `backend` | 백엔드 코끼리 |
| `fullstack` | 풀스택 코알라 |
| `data` | 데이터 펭귄 |
| `mobile` | 모바일 병아리 |
| `devops` | 데브옵스 물개 |
| `gamedev` | 게임 개발 새 |
| `ai` | AI 개발 나무늘보 |

**Response**

- Status: `200 OK`

```json
{
  "type": "frontend",
  "emoji": "🐹✨",
  "name": "프론트엔드 벌",
  "title": "예쁜 걸 사랑하는 디자이너 벌",
  "description": "사용자가 보는 화면을 아름답게 만드는 것이 가장 큰 즐거움!...",
  "characteristics": [
    "디테일에 강한 관찰력을 가지고 있다",
    "UI/UX에 대한 감각이 뛰어나다"
  ],
  "hashtags": ["#React", "#CSS", "#디자인", "#UI", "#사용자경험"]
}
```

**Error Response**

- Status: `404 Not Found`

```json
{
  "error": "해당 개발자 타입을 찾을 수 없습니다."
}
```

---

## 4. 테스트 결과 제출

### `POST /api/submit`

사용자의 답변을 제출하고 가장 많이 선택된 개발자 타입의 결과를 반환합니다.

**Request Headers**

| 헤더 | 값 |
|------|-----|
| `Content-Type` | `application/json` |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `answers` | string[] | O | 각 질문에서 선택한 개발자 타입 배열 |

```json
{
  "answers": ["frontend", "backend", "data", "fullstack", "ai", "data", "ai", "mobile", "data", "devops"]
}
```

**Response**

- Status: `200 OK`
- 가장 많이 선택된 타입의 결과를 반환합니다.

```json
{
  "type": "frontend",
  "emoji": "🐹✨",
  "name": "프론트엔드 벌",
  "title": "예쁜 걸 사랑하는 디자이너 벌",
  "description": "사용자가 보는 화면을 아름답게 만드는 것이 가장 큰 즐거움!...",
  "characteristics": [
    "디테일에 강한 관찰력을 가지고 있다",
    "UI/UX에 대한 감각이 뛰어나다"
  ],
  "hashtags": ["#React", "#CSS", "#디자인", "#UI", "#사용자경험"]
}
```

**Error Responses**

- Status: `400 Bad Request` — 배열이 비어있거나 누락된 경우

```json
{
  "error": "answers 배열이 필요합니다."
}
```

- Status: `400 Bad Request` — 유효하지 않은 타입이 포함된 경우

```json
{
  "error": "유효하지 않은 타입입니다: invalidType"
}
```
